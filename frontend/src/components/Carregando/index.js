import React from "react";
import { Container, Spinner } from "./styled";

const Carregando = () => {
  return (
    <Container>
      <Spinner></Spinner>
    </Container>
  );
};

export default Carregando;
